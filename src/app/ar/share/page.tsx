import clsx from "clsx";
import React from "react";

const SharePage = () => {
  return (
    <div
      className={clsx(
        "w-full h-full",
        "home-background-gradient"
      )}
    ></div>
  );
};

export default SharePage;
