import clsx from "clsx";
import React from "react";

const About = () => {
  return (
    <div
      className={clsx(
        "w-full h-full",
        "bg-yellow-100",
        "flex justify-center items-center"
      )}
    >
      About
    </div>
  );
};

export default About;
